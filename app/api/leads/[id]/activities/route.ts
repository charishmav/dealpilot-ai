import { NextResponse } from 'next/server'; import { addActivity } from '@/lib/repository';
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const b=await req.json();if(typeof b.title!=='string'||!b.title.trim())return NextResponse.json({error:'Activity title is required'},{status:400});const l=addActivity(id,{type:typeof b.type==='string'?b.type:'Note',title:b.title.trim(),description:typeof b.description==='string'?b.description.trim():''});return l?NextResponse.json(l):NextResponse.json({error:'Lead not found'},{status:404});}
