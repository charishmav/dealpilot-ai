import { NextResponse } from 'next/server'; import { addTask,setTask } from '@/lib/repository';
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const b=await req.json();if(typeof b.title!=='string'||!b.title.trim()||typeof b.dueDate!=='string'||!b.dueDate)return NextResponse.json({error:'Task title and due date are required'},{status:400});const l=addTask(id,{title:b.title.trim(),dueDate:b.dueDate});return l?NextResponse.json(l):NextResponse.json({error:'Lead not found'},{status:404});}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const b=await req.json();if(typeof b.taskId!=='string')return NextResponse.json({error:'taskId is required'},{status:400});const l=setTask(id,b.taskId,Boolean(b.completed));return l?NextResponse.json(l):NextResponse.json({error:'Lead not found'},{status:404});}
